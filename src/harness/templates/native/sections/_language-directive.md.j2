{# Language directive — enforces response language for all artifacts.
   Uses: artifact_lang (from Layer 0)
#}
{% if artifact_lang == "zh" %}
## 语言要求

**你必须使用中文回答所有内容。** 所有分析、发现、建议、总结和描述性文本都必须使用中文。
**语言统一规则（重要）**：安装并选定语言后，所有可控交互文案必须尽量统一为该语言。
- 工具调用中的 `description` 文案必须使用中文
- 阶段提示、进度更新、总结小节标题必须使用中文
- 仅在以下情况允许英文：命令/路径/代码、结构化关键字、专有技术术语、UI 固定不可控文案

例外：以下内容保持英文不变：
- 结构化输出中的字段名和关键字（如 `Score`、`Verdict`、`PASS`、`CRITICAL`、`WARN`、`INFO`）
- 代码片段、文件路径、命令行指令
- 专有名词和技术术语（如无通用中文译名）
{% elif artifact_lang == "en" %}
## Language Requirement

**You MUST respond entirely in English.** All analysis, findings, recommendations,
summaries, and descriptive text must be in English.
**Language consistency rule (important):** once an install language is selected, keep all controllable interaction copy in that language.
- Use English for tool-call `description` text
- Use English for stage prompts, progress updates, and section headings
- English exceptions remain allowed for commands/paths/code, structured keywords, proper nouns, and UI-fixed copy outside agent control

Exceptions — keep as-is:
- Structured output field names and keywords (`Score`, `Verdict`, `PASS`, `CRITICAL`, etc.)
- Code snippets, file paths, CLI commands
- Proper nouns and technical terms
{% endif %}
