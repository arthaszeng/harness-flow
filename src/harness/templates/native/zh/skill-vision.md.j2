---
name: harness-vision
description: "多数单任务请优先 `/harness-plan`。愿景驱动管线：探索模糊想法或澄清明确方向，通过多角色评审生成计划，然后自动执行 build/eval/ship。当用户说 vision、brainstorm、我有个想法、帮我想想、探索一下、我想朝这个方向、这是我需要的或 harness vision 时使用。(harness)"
---

{% include 'sections/_language-directive.md.j2' %}

# Harness Vision — 从想法到 PR

你正在运行 `/harness-vision` 工作流。用户有一个方向——
从**模糊的想法**到**清晰的增量愿景**不等——需要探索或澄清后才能成为计划。

**人机交互阶段：** 发散探索或快速澄清 + Plan Review Gate
**自主阶段：** Roadmap / Backlog / Active Plan Loop → Build → Eval → Ship → Feedback

## 工作流边界

- 完成 vision 后继续留在 harness 工作流里；不要调用 Cursor 的 `SwitchMode` 切到 `plan`。
- 规划阶段应直接使用本技能中内嵌的 harness 规划流程（等价于 `/harness-plan`），然后在**同一对话**内继续 **`/harness-build` → `/harness-ship`**（`/harness-ship` 不写业务代码；见内嵌计划的「执行」节）。
- 这里的"继续规划"指下一个 workflow 阶段，不是切换到 Cursor 内置的 Plan 模式。

**仅在以下情况停止：**
- 发散探索（与用户互动挖掘真实需求）
- 愿景澄清（如果方向有歧义）
- Review gate 升级（当 plan_review_gate 要求时）
- 评审中 ASK 类发现
- 用户明确喊停

**绝不因以下原因停止：**
- 实现决策（在 build 阶段做出）
- AUTO-FIX 发现（静默修复）

---

{% with preflight_mode="entry" %}
{% include 'sections/_workspace-preflight.md.j2' %}
{% endwith %}

---

## 阶段 0：理解与探索

评估用户输入，确定合适的探索深度。

### 步骤 1：判断清晰度

1. 仔细阅读用户的输入
2. 读取 `.harness-flow/vision.md`（如存在）以了解已有上下文
3. 读取 `.harness-flow/config.toml` 获取项目配置

**如果输入模糊**（例如"我有个想法"、"帮我想想 X"、
"我们探索一下"、只有问题描述没有解决方向）：
→ 继续**步骤 2A：发散探索**

**如果输入是清晰方向**（例如具体功能需求、明确技术方向、"我想做 X"）：
→ 跳到**步骤 2B：快速澄清**

### 步骤 2A：发散探索

此路径通过结构化对话帮助用户挖掘真实需求。

向用户提出 **2–3 个问题**来理解问题空间（不要一次全问完）：

第一轮：
- 你想解决什么问题？为谁解决？
- 当前情况是什么样的？痛点在哪？

第二轮（用户回复后）：
- 成功是什么样子？有什么约束？
- 你之前尝试过什么？什么有效什么无效？

然后根据用户的回答，生成 **2–3 个方案选项**：

```
APPROACH OPTIONS
═══════════════════════════════════════════════════
  #  Approach                  Pros                      Cons              Complexity
  1  [name]                    [short]                   [short]           Low/Med/High
  2  [name]                    [short]                   [short]           Low/Med/High
  3  [name]                    [short]                   [short]           Low/Med/High
═══════════════════════════════════════════════════
Which approach resonates? Or combine/remix ideas?
```

让用户选择、组合或提出替代方案。这是对话，不是测验。

用户选定方向后，继续到下方的"更新愿景"。

**Review gate 说明：** 当走了发散探索路径时，在评估 review gate 评分表时按 `interaction_depth = high` 计算。

### 步骤 2B：快速澄清

如果愿景有**模糊之处**，在一条消息中提出 **1–2 个澄清问题**。
不要与用户进行多轮问答 — 保持简洁。

需要澄清的情况示例：
- 范围不清晰（"改善认证"是指添加 MFA、修复 bug 还是重构？）
- 信号矛盾（愿景说"保持简单"但请求很复杂）
- 缺少上下文（引用了你在代码库中未见过的系统）

如果愿景足够清晰可以开始规划，跳过澄清直接继续。

**Review gate 说明：** 当走了快速澄清路径时，在评估 review gate 评分表时按 `interaction_depth = medium` 计算。

---

{# Vision = Phase 0 + vision-content (vision-core → vision-loop with plan lifecycle) #}
{% with interaction_depth="medium" %}
{% include 'sections/_vision-content.md.j2' %}
{% endwith %}

---

{% include 'sections/_bypass-immunity.md.j2' %}
