<!-- DO NOT EDIT — generated by harness init -->
# 构建测试分类与错误恢复

## 测试失败归属分类

对每个失败的测试，确定归属：

**预存在的失败**（在 `{{ trunk_branch }}` 上也失败）：
- 记录在构建日志中：「预存在的失败：test_name — 非本次变更引入」
- 继续 — 这不是你的问题

**分支内失败**（在基准分支通过，在 HEAD 失败）：
- 这是你的问题
- 仔细阅读失败输出
- 修复你的实现中的问题
- 重新运行 `{{ ci_command }}`
- 每个失败测试最多 3 次修复尝试

**分类后：** 若分支内失败在 3 次尝试后仍存在，在构建日志中记为阻塞项并停止。

## 错误恢复矩阵

注意：测试失败的分类（预存在 vs 分支引入）在上面处理。
此矩阵仅覆盖非测试错误。

| 错误类型 | 信号 | 恢复操作 |
|-----------|------|----------|
| CI 失败（lint） | linter 错误输出 | 如 linter 支持自动修复，修复后提交并重跑 |
| CI 失败（类型检查） | type checker 错误 | 修复类型注解后重跑 |
| Git 冲突 | merge conflict 标记 | 报告给用户，不要尝试解决 |
| 范围蔓延 | diff 涉及合约之外的文件 | 撤销未提交的变更，重新阅读合约 |
| 导入错误 | ModuleNotFoundError 或类似 | 检查是否缺少依赖，添加到 requirements |
| 权限错误 | PermissionError 或 EACCES | 报告给用户，不要重试 |
| Agent 超时 | 长时间没有输出 | 取消并使用更简单的 prompt 重试 |

如果同一错误类型在使用相同修复策略重试 2 次后仍然存在，
将发现写入 `.harness-flow/tasks/<task-id>/build-notes.md` 并继续下一个交付物。

## 中断后恢复

如果输出被截断或会话中断：
1. 读取 `harness session read --task <task-id>` 获取当前状态
2. 读取 `harness handoff read --task <task-id>` 获取阶段上下文
3. 从记录的确切步骤恢复 — 不要回顾或道歉
4. 如果剩余工作量大，拆分为更小的步骤
5. 不要重新运行已成功的步骤（检查 git log）

## 失败模式记录

当 CI 测试失败、lint 检查失败或遇到任何可复现的错误时，在继续之前**先记录**：

```bash
harness save-failure --json \
  --task task-NNN \
  --phase build \
  --category <ci-failure|test-failure|lint-error|type-error|build-error|runtime-error|other> \
  --summary "失败的简短描述" \
  --error-output "相关错误输出（前 500 字符，不含密钥）" \
  --root-cause "已知的根因" \
  --fix "已应用的修复"
```

{% include 'sections/_failure-pattern-sync.md.j2' %}
