---
name: harness-build
description: "按照 harness 计划/合约实现代码。读取计划，实现所有交付物，运行 CI，分类失败，并写入结构化构建日志。完全自主 — 无需确认。当用户说 build this、实现计划或 harness build 时使用。(harness)"
---

{% include 'sections/_language-directive.md.j2' %}
{% include 'sections/_git-strategy-matrix.md.j2' %}
{% include 'sections/_continuity-mandate.md.j2' %}

# Harness Build

你正在运行 `/harness-build` 工作流。这是一个**非交互式、完全自动化**的
构建管线。在任何步骤都不要请求确认。用户说了 `/harness-build`，
意味着去做。实现一切并输出构建结果。

**仅在以下情况停止：**
- 分支内测试失败在分类后无法自动修复
- 合约交付物确实无法实现（缺少依赖、不可行的需求）

**绝不因以下原因停止：**
- 实现方案确认
- "我应该继续吗？"的问题
- 预存在的测试失败
- 次要的实现决策（做出决定，记录在构建日志中）

---

{% include 'sections/_workflow-progress-envelope.md.j2' %}

## 核心原则

{{ builder_principles }}

---

## 步骤 1：阅读合约

1. **预检合集：**
   ```bash
   harness preflight-bundle --phase build --json
   ```
   使用 `task.task_dir` 作为 `TASK_DIR`。如会话上下文存在，从记录的步骤恢复。
   降级方案（如 bundle 失败）：`harness task resolve --json`、`harness handoff read --task task-NNN --json`、`harness session read --task task-NNN --json`。
2. 读取 `TASK_DIR/plan.md` — 从 Contract 部分提取交付物清单
3. 如果是后续轮次，同时读取 `TASK_DIR/code-eval-rN.md` 获取修复建议
4. 如果 `TASK_DIR/workflow-state.json` 存在，视为 phase/gate 的 canonical 来源

---

## 步骤 2：构建前搜索

在编写代码之前，先复用 `handoff-plan.json` → `context_footprint` 中的路径（如存在）。扫描已有模式、工具函数和测试约定。仅在有缺口时扩大搜索。

{% include 'sections/_failure-pattern-prereview.md.j2' %}

**会话边界：** 通过 `harness session write --task task-NNN` 更新会话，步骤 `2/7`。

---

## 步骤 2.5：设计原则验证（强制）

如果 `plan.md` 包含 `## Design Principles` section：

1. 逐条读取 DP 条目
2. 对每条原则，确认你的实现方案能满足它
3. **如果任何 DP 不被满足，停下来重新设计方案，而非继续写代码**
4. 在构建日志（步骤 5）中记录每条 DP 的满足确认

**豁免条件：** 如果 `plan.md` 没有 Design Principles section（如纯文档或热修复任务），跳过此步。

---

## 步骤 3：实现

对合约中的每个交付物：

1. 实现代码变更
2. 为新行为编写测试（匹配项目已有的测试模式）
3. 在项目使用类型提示的地方确保类型提示/类型安全
4. 处理错误路径 — 不要留下裸异常或缺失的错误处理

**约束：**
- 仅修改合约交付物直接要求的或隐含的文件
- 绝不删除已有测试 — 添加新测试，不要移除旧的
- 绝不修改交付物中未列出或未隐含的文件
- 如果发现合约范围之外的相关问题，记录在构建日志中但不要修复

{% if project_lang == 'python' %}

### Python 特定指导
- 提交前运行 `ruff check`；使用 `ruff check --fix` 自动修复
- 使用类型提示（PEP 484）；如果项目使用 `mypy` 则运行它
- 优先使用 `pathlib.Path` 而非 `os.path`
- 使用 `from __future__ import annotations` 获取现代类型语法
{% elif project_lang == 'typescript' %}

### TypeScript 特定指导
- 提交前运行 `tsc --noEmit` 进行类型检查
- 优先在 tsconfig 中使用 `strict: true`
- 对不可变属性使用 `readonly`
- 对象类型优先使用 `interface` 而非 `type`
{% elif project_lang == 'go' %}

### Go 特定指导
- 提交前运行 `go vet ./...` 和 `staticcheck ./...`
- 使用 `errcheck` 检查未处理的错误
- 提交前运行 `gofmt` / `goimports`
{% elif project_lang == 'rust' %}

### Rust 特定指导
- 提交前运行 `cargo clippy`
- 运行 `cargo fmt --check` 检查格式
{% endif %}

---

## 步骤 4：运行 CI

运行完整的测试套件：

```bash
{{ ci_command }}
```

### 测试失败分类

如果测试失败，分类：**预存在**（在 `{{ trunk_branch }}` 上也失败 → 记录并继续）vs **分支内**（你的问题 → 修复，最多 3 次尝试）。

**出现任何失败时，必须先阅读** `.cursor/skills/harness/harness-build/build-test-triage.md` 获取完整分类协议、错误恢复矩阵和失败模式记录说明。

---

## 步骤 5：写入构建日志

**使用 CLI 保存构建日志**（必须执行 — 不可跳过）：

```bash
harness save-build-log --task task-NNN --body "$(cat <<'BUILD_EOF'
# Build Log — Round N

## Deliverables
- [x] D1: what was done
- [ ] D2: NOT DONE — reason

## Design Principles Verification
- DP1: satisfied — (how)

## CI Result
PASS / FAIL — N passed, M failed (K pre-existing)

## Blockers
(none, or list)
BUILD_EOF
)"
```

将 `task-NNN` 替换为实际值，填入真实内容。
命令会以自动递增的轮次编号创建 `build-rN.md`。
如果 `workflow-state.json` 已存在，也要同步更新。

{% if hooks_pre_build or hooks_post_eval or hooks_pre_ship %}
---

{% include 'sections/_hook-points.md.j2' %}

---
{% endif %}

## 步骤 5.5：写入构建交接摘要

如果 `workflow-state.json` 存在，将构建结果（≤ 2000 字符）写入其 `handoff_summary` 字段。

## 步骤 5.6：写入 `handoff-build.json` — 强制

在提交前写入 `TASK_DIR/handoff-build.json`，包含：`schema_version: 2`、`source_phase: build`、`target_phase: eval`、`task_id`、`summary`、`decisions`、`risks`、`open_items`、`artifacts_produced`、`scope_changes`、`context_footprint`（`explored_paths`、`primary_read_files`、`primary_touched_files`）。

## 步骤 6：提交

暂存并提交实现：

```bash
git add <specific-files-changed>
git commit -m "<type>(scope): implement task-NNN deliverables

Deliverables: (list what was implemented)
CI: all tests pass"
```

使用具体的文件路径 — 绝不使用 `git add -A` 或 `git add .` 以避免包含无关变更。

## 步骤 7：继续 Ship（同一对话）

实现提交成功且 CI 通过后，必须在**同一对话**内**立即**继续执行 **`/harness-ship`**。
不要结束回合等待用户再说「ship」或「继续」。

`/harness-ship` 负责测试、**自适应多角色代码评审**（基于 escalation score 自动选择 FAST/LITE/FULL）、`harness save-eval`、`harness gate`、推送与 PR — **不**替代你在步骤 6 已提交的实现代码。
