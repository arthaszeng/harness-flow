### 先验失败模式

开始工作前，检查之前任务中的已知失败模式：

```bash
harness search-failures --query "<相关关键词>" --limit 10
```

审查结果中是否有可能影响本次任务的反复出现的问题。如果先验模式提示了已知陷阱，将其纳入你的方案。

{% if memverse_enabled == "true" %}
**Memverse 语义搜索：** 如需更广泛的跨项目模式匹配，同时查询 Memverse：

```
CallMcpTool(
  server="user-Memverse",
  toolName="search_memory",
  arguments={
    "query": "<相关关键词>",
    "domains": "{{ memverse_domain }}",
    "metadata_filter": "{\"type\": \"failure-pattern\"}",
    "limit": 10
  }
)
```
{% endif %}
