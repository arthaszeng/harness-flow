## 计划评审 — 5 角色并行派遣

### 派遣

通过 Task 工具**并行启动所有 5 个审查者**，每个使用 `mode: plan-review`。
发送一条包含所有 5 个 Task 工具调用的消息：

| # | 子代理 | 推荐模型 | 必需 Context | 可选 Context |
|---|--------|---------|-------------|-------------|
| 1 | `harness-architect` | {% if role_models_architect %}不指定（继承父代理模型）{% else %}`model: "fast"`{% endif %} | plan、架构文档（`ARCHITECTURE.md`/ADR） | 源码目录概览 |
| 2 | `harness-product-owner` | {% if role_models_product_owner %}不指定（继承父代理模型）{% else %}`model: "fast"`{% endif %} | plan、`.harness-flow/vision.md` | roadmap/backlog 摘要 |
| 3 | `harness-engineer` | {% if role_models_engineer %}不指定（继承父代理模型）{% else %}`model: "fast"`{% endif %} | plan、源代码目录结构概览 | — |
| 4 | `harness-qa` | {% if role_models_qa %}不指定（继承父代理模型）{% else %}`model: "fast"`{% endif %} | plan、测试目录列表、`ci_command: {{ ci_command }}` | — |
| 5 | `harness-project-manager` | {% if role_models_project_manager %}不指定（继承父代理模型）{% else %}`model: "fast"`{% endif %} | plan、交付物数量、文件影响数量 | roadmap/backlog/stop-rule 摘要 |

**模型控制**：Task 工具 `model` 参数仅接受 `"fast"` 或省略（继承父代理模型）。
当 `[native.role_models]` 为某角色配置了模型时，生成的 agent 定义会包含 `model:` 字段
作为 IDE 级别的信号；此时省略 Task `model` 参数以让 agent 定义生效。
未配置时，传递 `model: "fast"` 以实现低成本派遣。

**Context 最小化**：构建每个子代理的 Task prompt 时，**仅包含 agent 模板中 "评审计划时（mode: plan-review）" 段落**
作为角色指令。不要嵌入 "评审代码时" 段落——那是 code-eval 模式专用的。

每个 prompt 必须包含：
- `response_language: {{ artifact_lang }}`
{% if memverse_enabled == "true" %}
- `memverse_domain: {{ memverse_domain }}`
{% endif %}
- 上表中该角色的"必需 Context"
- 当工作流包含长期方向治理时：
  - 对 PO 额外提供 `roadmap_backlog_path` 或等价摘要
  - 对 PM 额外提供 `roadmap_backlog_path`、`stop_conditions` 或等价摘要
  - 如果这些上下文不存在，则保持共享 section 的默认单轮语义，不要强行假设 loop

**降级阶梯**（如果 Task 工具调用失败）：
- **5/5 响应：** 按下述方式完整合成
- **3-4/5 响应：** 使用可用的评审结果继续，注明缺失的视角
- **1-2/5 响应：** 记录警告，降级为单代理评审
- **0/5 响应：** 回退到单个 generalPurpose 子代理评审

### 合成

所有响应到达后：

1. **合并发现** — 将所有 5 位审查者的发现合并为统一列表
2. **过滤超范围发现** — 使用角色→职责映射：
   | 角色 | 负责 | 不得报告 |
   |------|------|---------|
   | Architect | 设计、模块、依赖、安全设计 | 代码质量、测试、交付、用户价值 |
   | Product Owner | 用户价值、需求、AC、UX 边界情况 | 代码质量、架构、测试、交付 |
   | Engineer | 代码质量、DRY、模式、性能、错误处理 | 架构决策、用户价值、交付、测试策略 |
   | QA | 测试、覆盖率、CI、回归、边界 | 代码质量、架构、用户价值、交付 |
   | Project Manager | 范围、交付、分解、提交结构 | 代码质量、架构、测试、用户价值 |

   对每个发现，检查报告角色是否负责该类别：
   - **范围内**：保持原样
   - **范围外 + CRITICAL**：重新标记为 `[CROSS-ROLE]` 并保留
   - **范围外 + WARN/INFO**：标记 `[OUT-OF-SCOPE]` 并从评分中排除
3. **交叉验证：** 如果 2+ 个角色标记同一问题 → 标记 `[HIGH CONFIDENCE]`
4. **方向/治理合成：** 保持角色建议字段并列，不要合并为单一方向字段：
   - Product Owner → `Value recommendation`
   - Project Manager → `Delivery recommendation`
5. **解决冲突：** 如果角色之间相互矛盾，呈现双方观点
   并标记为 `[TASTE DECISION]` 交由 review gate 处理
6. **计算综合评分：**
   ```
   Architecture:  [architect score]/10
   Product:       [product-owner score]/10
   Engineering:   [engineer score]/10
   Testing:       [qa score]/10
   Delivery:      [project-manager score]/10
   ─────────────────────────────────
   Weighted avg:  [avg]/10
   ```
7. **确定裁定：**
   - 全部 PASS 且 avg ≥ {{ pass_threshold }} → `PLAN_APPROVED`
   - 任何 CRITICAL 发现 → `PLAN_NEEDS_REVISION`
   - avg < {{ pass_threshold }} → `PLAN_NEEDS_REVISION`

### 重新规划循环（最多 2 次迭代）

如果裁定为 `PLAN_NEEDS_REVISION`：

1. 修复计划文件中所有 CRITICAL 和 WARN 发现
2. 重新派遣完整的 5 角色评审（相同的并行模式）
3. 如果相同问题再次出现（收敛保护）→ 接受并记录为 `## Unresolved Concerns`
4. 2 次迭代后 → 以当前状态进入 review gate

将最终合成结果持久化为计划文件中的 `## Plan Review Summary`。

**使用 CLI 保存计划评审结果**（用于轮次审计，必须执行）：

```bash
harness save-eval --kind plan --task task-NNN --verdict PASS --score 8.5 --body "$(cat <<'PLAN_EVAL_EOF'
# Plan Evaluation — Round N

## Findings
[merged plan-review findings]

## Verdict: PASS | ITERATE
PLAN_EVAL_EOF
)"
```

该命令会创建 `plan-eval-rN.md`，并与 code-eval 共用同一个自动递增轮次计数。
