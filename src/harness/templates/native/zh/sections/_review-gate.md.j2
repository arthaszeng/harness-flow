## Review Gate — {{ plan_review_gate }}
{% if plan_review_gate == "human" %}

**停止。** 将完整的计划和评审报告呈现给用户。

展示：
1. 完整计划（spec + 合约）
2. 计划评审摘要（评分、综合分、发现）
3. 任何未解决的审查者关切

等待用户响应：
- **approve** → 继续进入实现
- **edit: [反馈]** → 返回规划阶段，根据反馈重新生成
- **reject** → 终止工作流
{% elif plan_review_gate == "auto" %}

通过 CLI 计算**升级评分**来确定计划是否需要人工审查：

```bash
harness escalation-score compute --phase plan --json
```

CLI 读取计划元数据（交付物数量、预计文件数、安全/schema/API 标志、评审分数、交互深度、信任调整），
返回 `{score, level, signals}`。

**此入口的默认交互深度：{{ interaction_depth | default("low") }}**
（若阶段 0 选择了不同路径，以该路径的深度为准——见阶段 0 的 review gate 说明。）

### 评审强度路由

**Level = FULL（score >= {{ gate_full_review_min }}）→ 完整审查**
呈现完整计划 + 评审摘要。等待：
- **approve** → 继续
- **edit: [反馈]** → 重新规划
- **reject** → 终止

**Level = LITE（score {{ gate_summary_confirm_min }}–{{ (gate_full_review_min | int) - 1 }}）→ 摘要确认**
展示简要摘要：
```
PLAN SUMMARY
═══════════════════════════════════════════
  Task:          task-NNN
  Deliverables:  N items
  Files:         ~N estimated
  Risk flags:    [list or "none"]
  Escalation:    score N / level LITE
  Aggregate:     X/10
═══════════════════════════════════════════
Continue? [approve / edit / reject]
```

**Level = FAST（score < {{ gate_summary_confirm_min }}）→ 自动通过**
记录：`"Plan auto-approved (escalation score N, level FAST, aggregate review score X/10)"` 然后继续。
如果任务级 `workflow-state.json` 存在，请同步写入本次 plan-review gate snapshot。
{% else %}
计划在完成评审后自动批准。立即继续。
{% endif %}
